package com.team13.dealmymeal.core

import com.team13.dealmymeal.data.Meal

class PlanGenerator {
    companion object {
        fun generatePlan(days: Int, mealsPerDay: Int, meat: Int, vegetarian: Int, special: Int): List<Meal> {
            return emptyList();
        }
    }
}